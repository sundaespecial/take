{
  "name": "say-so-app",
  "version": "0.4.0",
  "private": true,
  "description": "SAY SO — dictation instrument for ticket work. On-device Whisper transcription, deterministic field extraction, ephemeral share links.",
  "scripts": {
    "start": "npx http-server -p 8080 -c-1 .",
    "keygen": "node keygen.mjs",
    "test": "node --test && node tools/verify.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  }
}
