{
  "name": "take-app",
  "version": "0.2.0",
  "private": true,
  "description": "TAKE — dictation instrument for ticket work. On-device transcription, deterministic field extraction, sealed sync.",
  "scripts": {
    "start": "npx http-server -p 8080 -c-1 .",
    "keygen": "node keygen.mjs"
  }
}
